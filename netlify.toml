[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Client-side routes (React Router); without this, /dashboard etc. return 404 on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
